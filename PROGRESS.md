# AIONIX Market — Progress Log

Persistent log so any session can resume. Newest entries at the bottom of each section.

## Architecture (decided 2026-09-13)
- **Monorepo** pnpm workspaces + Turborepo.
  - `apps/web` — PWA do consumidor (Next.js 16 App Router) → Vercel project `aionix-market` (`prj_GCzD4BfpC3Q0iIcxhi619tNE1IJP`, team `aionixdev`). Prod: https://aionix-market.vercel.app
  - `apps/admin` — Painel administrativo (Next.js 16) → Vercel project `aionix-market-admin` (`prj_ZkfPdXjoYBvNs5kOYpOu9ooFYTTi`). Prod: https://aionix-market-admin.vercel.app
  - `apps/api` — Fastify 5 + Drizzle ORM + Postgres → Railway project `aionix-market` (service `api`, `Postgres`, bucket `aionix-media`). Prod: https://api-production-5de6.up.railway.app
  - `packages/shared` — zod schemas, tipos, formatadores (consumido como TS source; `transpilePackages` no Next, `noExternal` no tsup).
- **Auth** self-hosted: argon2 hashes, access JWT (15 min) + refresh token opaco rotativo (tabela `sessions`), cookies httpOnly `ax_at`/`ax_rt` com `path=/api`.
  Frontends fazem rewrite `/api/*` → Railway (`API_ORIGIN` env), então cookies são first-party.
- **Realtime**: SSE direto do Railway (`/api/realtime/stream?ticket=`), ticket JWT de 90s emitido via `/api/realtime/ticket` (mesma origem).
- **Mídia**: upload → sharp (WebP ≤1200px + blur placeholder 16px) → S3 (Railway bucket) se `S3_*` configurados, senão tabela `media_blobs`. Servido em `/api/media/<key>` com cache imutável.
- **Preços**: `lib/pricing.ts` é a fonte da verdade. Promoções (percent/fixed, por produto ou categoria) aplicadas no servidor; carrinho do cliente é re-cotado via `POST /api/cart/quote`.
- **Pedidos**: fluxo `pending → confirmed → picking → out_for_delivery → delivered`, `cancelled` a partir de qualquer não-final. Estoque decrementado atomicamente no checkout, devolvido no cancelamento.
- **Pagamento**: na entrega (Pix/cartão/dinheiro com troco). Gateway online está em BLOCKERS.md.
- **Migrations**: `pnpm --filter @aionix/api db:generate` gera SQL em `apps/api/drizzle`; a API aplica no boot (`migrate()`).

## Accounts / infra
- GitHub: `alanaraujo-bit/AIONIX-market` (branch `main`; push dispara Vercel e Railway).
- Railway: workspace "alanaraujo-bit's Projects", project `e3d26e9f-7661-4f83-ade9-cc0ef2f8c914`. Postgres TCP proxy: `altaria.proxy.rlwy.net:25486`.
- Vercel: user `alanarauj0`, team `aionixdev` (`team_nBRZLjwUxaJG1SB3JxLq8l92`). Env `API_ORIGIN` aponta para o Railway.
- Credenciais do admin: `CREDENTIALS.local.md` (git-ignored). Demo cliente: `cliente@aionix.market` / `aionix2026`.

## Local dev
- **Ambientes Railway**: `production` (Postgres `altaria.proxy.rlwy.net:25486`) e `staging` (Postgres-Me7v, proxy `hayabusa.proxy.rlwy.net:28181`, env id `3bae5d31-4a17-4613-b103-8ad5a1b3d80f`). Dev local e E2E usam **staging** — nunca gravar pedidos/estoque de teste em produção. O bucket S3 é compartilhado (mídia não é transacional).
- API: `pnpm -C apps/api dev` (porta 8080, usa `apps/api/.env` → staging + S3).
- Web: `pnpm -C apps/web dev -p 3000`. Admin: `pnpm -C apps/admin dev -p 3001`.
- Seed staging: `cd apps/api && pnpm exec tsx --env-file=.env src/db/seed.ts` (idempotente por slug; `--reimage` reprocessa imagens). Produção: `--env-file=.env.prod` (git-ignored).
- QA: `node scripts/shot.mjs <url> <out.png> [--login] [--scroll N]`; E2E `node scripts/flow.mjs http://localhost:3000 <dir>`; realtime `node scripts/realtime-e2e.mjs`; PWA `node scripts/pwa-check.mjs <origin>`; testes `pnpm turbo test`; tipos `pnpm turbo typecheck`.

## Log
- 2026-09-13: Repo inicializado, monorepo scaffold, Railway project + Postgres + bucket, API deployada (health OK).
- 2026-09-13: Vercel projects criados e ligados ao GitHub (root dirs `apps/web`, `apps/admin`).
- 2026-09-13: Catálogo seedado (129 produtos, 12 categorias, 2 promoções, 3 banners) com imagens do Open Food Facts / Wikimedia Commons reprocessadas para o bucket.
- 2026-09-13: PWA do consumidor completa: home, busca, categoria, produto, carrinho, checkout, pedidos (timeline realtime), conta, endereços. Fluxo E2E validado via Playwright.
- 2026-09-13: PWA: manifest, service worker (offline + cache de mídia), ícones gerados (`scripts/icons.mjs`).
- 2026-09-13: Fix Vercel: `API_ORIGIN` precisava estar em `turbo.json` (`globalEnv`/`env`), senão o rewrite `/api` caía. Mídia agora usa URLs relativas `/api/media/...`.
- 2026-09-13: Admin completo (login, dashboard c/ gráfico, fila de pedidos c/ SSE + som, detalhe do pedido, produtos c/ upload/inline stock/bulk, categorias DnD, promoções, banners c/ prévia, mídia, clientes, configurações). E2E realtime (`scripts/realtime-e2e.mjs`) verde. Produção validada em ambos os domínios.
- 2026-09-13: Revisão do advisor aplicada: (1) bug de dinheiro — desconto fixo sem teto vendia por R$ 0,01 → piso de 10% do preço em `applyDiscount` + validação no cadastro da campanha; (2) `quoteCart` dividido em `buildQuote` puro + 15 testes vitest; (3) `pwa-check.mjs` valida manifest/ícones/SW/offline em produção (tudo verde; adicionado meta `apple-mobile-web-app-capable`); (4) checkout usa `etaMinutes` das configurações; (5) botões mortos no detalhe do pedido removidos; (6) pipeline de imagem detecta fundo claro uniforme e o leva a branco (Maçã/Tomate/Abacaxi), foto do pimentão trocada; (7) ambiente `staging` no Railway para dev/E2E — produção deixou de receber pedidos de teste (os pedidos #00001–#00003 em produção são resíduo dos E2E anteriores).
- 2026-09-13: Contagem de produtos na categoria vem da query viva (não mais do ISR de 60s). README escrito.
- 2026-09-13: Seed re-run preserva `stock`/`active` (campos do admin). Hortifruti reprocessado em staging e produção (abacaxi/pimentão com fotos novas). Deploy validado: Railway `3ba2e58`, Vercel web+admin no HEAD, `pwa-check` 100% verde, E2E consumidor + realtime verdes contra staging.
- 2026-09-13: Pipeline de imagem: além do ganho pelos cantos, fundos cinza (luma ≤252) recebem fade de pixels neutros claros → sombra de estúdio deixa de aparecer como "placa" sob tomate/maçã/morango; alho (fundo 253) fica fora do fade e preserva detalhe. `scripts/image-sheet.ts` agora compõe sobre branco (igual ao card) — antes o contact sheet escondia o defeito. Nota: o primeiro `--reimage=hortifruti` em produção rodou antes da correção de `stock/active` no upsert e resetou esses campos aos valores do seed (ninguém tinha editado via admin; nada perdido).
- 2026-09-13: **Clube AIONIX (preço de clube)**, pedido do cliente ("preço normal pro cliente e preço de clube pros mais fiéis"). Modelo: `products.club_price_cents` (público, vai em todo payload do catálogo como *motivo para entrar*) + `users.club_member`/`club_joined_at` (sessão). Regra de dinheiro: membro paga `min(promoção, clube)`, só em `POST /cart/quote` e `POST /me/orders` (resolvido de `req.auth`) — respostas do catálogo continuam idênticas para guest/membro (validado), então o ISR de 30s não vaza preço. `order_items.via_club` guarda a origem; `/me` expõe `clubSavedCents`. Adesão: grátis em 1 toque (`POST /me/club/join`) ou toggle do admin em Clientes (`PATCH /admin/customers/:id`). App: tokens `club`/`club-gold` (roxo + dourado, distinto de sale/brand), pill "Clube R$ X" nos cards, card na PDP, seção + hero na home, `/clube`, sheet de adesão com confete, nudge no carrinho ("entre e economize R$ X neste pedido"), linha "Preço de Clube" no carrinho/checkout, card de membro na conta. Admin: campo no produto (com validação clube < venda), coluna/toggle em Clientes. Seed: 7 produtos com preço de clube (Aveia 6,99→4,99; Dolce Gusto 34,90→24,90; etc.). 8 testes novos em `pricing.test.ts`. Fix colateral: lista admin de produtos agora devolve `compareAtCents` bruto (antes o serializer da loja punha o preço de lista durante promoção e o formulário falhava ao salvar).
- 2026-09-13: **Regra do Clube fechada com o cliente**: "todos que estiverem cadastrados e comprarem pelo app" → todo cliente registrado é membro automaticamente (`club_member` default true, set no registro, migração `0003` faz backfill dos existentes). Removido o `POST /me/club/join` (adesão avulsa deixaria quem o admin tirou voltar sozinho); o toggle do admin em Clientes fica como exceção. No app, o convite virou "Criar conta e entrar no Clube" (sheet, hero da home, `/clube`, nudge do carrinho); após o cadastro o sheet reabre com a comemoração e os preços já trocados. Fluxo guest → cadastro → membro validado via Playwright.
- 2026-09-13: **Clube em produção.** Push `23459b4`/`b1d9e9a` → Railway aplicou `0002_club` + `0003_club_everyone` (backfill) no boot; Vercel web+admin no HEAD. Preços de clube dos 7 produtos foram gravados em produção via API do admin (`PATCH /admin/products/:id`), sem rodar o seed lá (o seed sobrescreveria campos de catálogo). Home de produção mostra a seção do Clube. Observação: a home em produção às vezes loga React #418 (hydration) no console — só na home, intermitente, sem efeito visual (React re-renderiza no cliente); texto SSR = CSR, não reproduz local nem em `next start`. Não investigado a fundo.
- 2026-09-13: **AIONIX Moedas (fidelidade)** — pedido do cliente: moedas virtuais por compra, trocáveis por prêmios totalmente configuráveis pelo dono.
  - **Modelo**: ledger append-only `coin_entries` (earn/bonus/redeem/refund/adjust/reversal; status pending→settled→void). Saldo = soma de `settled`. Moedas do pedido nascem `pending` e liquidam em `settings.loyalty.awardOn` (criado/confirmado/entregue); cancelamento anula pendentes, estorna liquidadas e devolve o voucher à carteira. `rewards` (desconto R$, desconto %, frete grátis, produto grátis, brinde de balcão; custo, pedido mínimo, estoque, limite por cliente, ativo/ordem). `redemptions` = voucher com código curto `AX-XXXX-XXXX` e snapshot congelado do prêmio. Migration `0004_loyalty`.
  - **Dinheiro no servidor**: `buildQuote` aplica o voucher depois de promoções/clube; moedas calculadas sobre o valor pago em produtos (sem frete). Checkout só aceita voucher que a cotação aceitou e o reserva na mesma transação. 17 testes novos (`loyalty.test.ts`).
  - **Configuração (admin `/fidelidade`)**: regras (N moedas a cada R$ X, mínimo, quando liberar, bônus Clube %, bônus cadastro, bônus 1º pedido, nome/emoji da moeda, frase), simulador, CRUD de prêmios com prévia do card do app, vouchers (filtro + validar código de brinde), visão geral (KPIs + gráfico 30d). Em Clientes: coluna de saldo + gaveta de extrato/ajuste manual. Pedido do admin mostra prêmio usado e moedas.
  - **App**: carteira `/moedas` (saldo com contador rolando, moeda SVG girando, barra até o próximo prêmio, vouchers, prêmios, histórico, toggle de som), sheet de resgate com moedas "saindo", voucher com código, seletor de voucher no carrinho/checkout, chip "este pedido rende N moedas", linha de moedas na tela de sucesso, card na home e na conta, badge no pedido. **Celebração**: overlay com chuva de moedas em arcos entrando na carteira, contador, vibração e som — disparada pelo SSE `coins.credited` ou, se o app estava fechado, pelas entradas `unseen` da carteira (marcadas como vistas ao fechar).
  - **Sons** (`lib/sound.ts`): sintetizados via WebAudio (sem arquivos): "ding" de moeda B5→E6 com parcial metálico suave, cascata pentatônica proporcional à quantidade, acorde de sucesso, pop e whoosh. Destravados no primeiro toque; preferência persistida.
  - **E2E**: `node scripts/coins-e2e.mjs http://localhost:3000 out/coins` (staging): resgate na UI → voucher no carrinho → checkout → admin entrega → celebração → saldo = antes − custo + ganho, voucher `used`, entradas vistas.
  - **Coordenação**: outra sessão trabalha em conquistas + retirada no mesmo working tree (ver `COORDINATION.md`).
- 2026-09-13: **Moedas — revisão do advisor aplicada**: (1) "Validar código de brinde" só aceita vouchers de brinde (antes queimava um voucher de desconto sem uso); (2) com `awardOn = criado`, moedas liquidadas no checkout já nascem vistas (sem celebração por cima da tela de sucesso) e a resposta do checkout traz o `coinsStatus` real ("na sua carteira" vs "a caminho"); (3) estorno por cancelamento nunca deixa saldo negativo (`reversalCoins` limita ao saldo; moedas já gastas ficam gastas) e ajuste manual negativo acima do saldo é recusado (409). +2 testes (51 no total). E2E verde de novo (pedido #14) e guardas verificadas via API. **Push para produção retido**: ver `BLOCKERS.md` item 2 (os commits incluem trabalho em andamento de conquistas/retirada com migration 0005 encadeada).
- 2026-09-13: **Conquistas + retirada na loja assumidas desta sessão** (o trabalho anterior era de outra IA, o Codex, que ficou sem créditos). Estado herdado validado: integração 15/15 e teste visual (mobile/desktop/admin) verdes. Correções: link "Conhecer os prêmios" apontava para `/premios` (inexistente) → `/moedas`; celebração da medalha invalidava a chave errada da carteira; bônus em moedas de conquista gerava celebração dupla (medalha + chuva de moedas) → nasce vista; métricas de valor ("compra de R$ 100", "R$ 500 acumulados") contavam a taxa de entrega → agora só produtos; aviso "Nova conquista" cobria o botão do carrinho → só aparece em telas de navegação; som de sucesso + vibração ao abrir a medalha; editor do admin ainda dizia que retirada não existia.
  - **Retirada ganhou a etapa "Pronto para retirada"**: antes o fluxo pulava de "Em separação" direto para "Retirado" e o cliente nunca sabia quando ir buscar. Agora pickup usa o mesmo fluxo com rótulos próprios (`orderStatusLabel/Short` em `@aionix/shared`): "Pronto para retirada" (ícone de loja, aviso em tempo real com som) e "Retirado na loja". Eventos SSE carregam `fulfillmentMethod`; admin tem "Avisar: pronto para retirada" e "Confirmar retirada pelo cliente". Textos de moedas também seguem o método ("retirado", "na retirada").
  - **E2E novo** `scripts/pickup-e2e.mjs` (staging, liga a retirada só durante o teste e restaura): pedido sem frete, etapas completas, medalha "Oi, pessoalmente!" + moedas creditadas.
  - **Notas herdadas consolidadas aqui** (arquivos `COORDINATION.md`, `ACHIEVEMENTS-*.md`, `PICKUP-PROGRESS.md` removidos — descreviam dois agentes em paralelo, o que não existe mais). Decisões de conquistas que continuam valendo: schema separado (`db/achievement-schema.ts`, migration 0005); 34 conquistas padrão em 11 métricas e 4 dificuldades, criadas uma vez por slug no primeiro acesso (edições/arquivamentos do admin preservados); XP em 6 níveis, nunca é moeda; bônus em moedas é opcional, passa pelo ledger e é creditado uma única vez (constraint única usuário+conquista + trava da linha do usuário compartilhada com fidelidade); só pedidos concluídos contam; calendário no fuso de São Paulo sem "sequência" que zera; métrica de moedas ignora bônus (sem efeito bola de neve); snapshot imutável da conquista no momento da premiação; reconciliação no GET do cliente e no evento de pedido concluído; conquistas de retirada ficam ocultas enquanto a retirada estiver desligada. Limitação conhecida: "categorias diferentes" usa a categoria atual do produto (itens de pedido não guardam a categoria histórica).

- **2026-09-13 — publicado em produção** (bb1d862): moedas, conquistas e retirada no ar; health ok, migrations 0004/0005 aplicadas, rotas web/admin 200. Pendências do dono: prêmios em /fidelidade e endereço de retirada.

## 2026-09-13 — Identidade sonora (app + painel)
- **Pacote `@aionix/sound`** (sem dependências, sem arquivos de áudio): síntese WebAudio com 4 timbres temáticos — madeira (toques), papel/ar (sacola, movimento), sinos FM (novidades) e metal inarmônico (só dinheiro) — tudo em Dó maior pentatônica. Master: HPF 90 Hz → shelf −4 dB @6,5 kHz → LPF 9 kHz → compressor suave; reverb curto sintetizado em send. 31 sons em 6 famílias, com prioridade (um gesto nunca gera dois sons; notícia não repete com o eco do tempo real; aba em segundo plano só toca alertas).
- **Qualidade medida** (`node scripts/sound-lab.mjs`): renderiza o bundle real offline, exige pico < −1 dBFS, sem DC, duração e loudness dentro da faixa de cada prioridade (dispersão ≤ 6 dB por faixa) e exporta um WAV por som em `out/sounds/`.
- **App**: sacola (nota sobe com a quantidade), remover/desfazer/esvaziar, seleções do checkout, assinatura "pedido feito", cada status do pedido (aceito, separando, a caminho, pronto p/ retirar, concluído, cancelado), moedas, troca, conquista, clube, login/logout, copiar, erros. Toasts tocam por tom. **Conta → Sons** liga/desliga.
- **Painel**: sino de balcão para novo pedido (toca com a aba em segundo plano; com várias abas, só uma), lembrete a cada 2 min enquanto houver pedido pendente, aviso "clique para ativar o alerta sonoro" quando o navegador bloqueia áudio, sons de status nas ações do pedido, exclusões, interruptores, reordenar, moedas/voucher no balcão. **Configurações → Sons e alertas**: volume, lembrete e a coleção de sons para ouvir.
- **Validação**: `scripts/sound-e2e.mjs` verifica o que foi realmente tocado (`window.__aionixSounds`) no app e no painel; flow, realtime, coins, pickup e pickup-settings seguem verdes.
